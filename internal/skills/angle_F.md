# Angle F — altitude & conventions

Special cases layered on shared infrastructure — a one-off branch where the
deeper, general fix belongs. Name the deeper fix. Enforce project convention
files (CLAUDE.md / AGENTS.md / REVIEW.md on the changed file's directory path)
**only** with the exact rule and the offending line quoted; a rule file that
doesn't cover the changed file's path does not apply. These are MINOR at most.
