# Angle G — conformance

Check the acceptance criteria and author intent in the brief against the diff:
a stated requirement with no corresponding change or test is a candidate;
scope drift (changes unrelated to the intent) is a candidate; a bug-fix with
no regression test guarding the bug class is a candidate. You cannot run the
tests — reason from the diff and the brief about what evidence is missing.
