# Repo conventions

## Conventions

- Every exported Go function, type, and method MUST have a doc comment whose
  first word is the identifier's name (Go's standard `// Name ...` form). A new
  exported identifier without such a doc comment is a violation.
